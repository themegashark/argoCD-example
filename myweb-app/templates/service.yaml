apiVersion: v1
kind: Service
metadata:
  name: {{ include "myweb-app.fullname" . }}
  labels:
    {{- include "myweb-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myweb-app.selectorLabels" . | nindent 4 }}
