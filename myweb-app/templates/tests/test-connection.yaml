apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "myweb-app.fullname" . }}-test-connection"
  labels:
    {{- include "myweb-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "myweb-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
